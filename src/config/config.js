const MQTT_PORT = 9000;
const BROKER_ID = "ZZK";
const URL = `mqtt://localhost:${PORT}`;
const SERVICE_OPTIONS = {
  clientId: "service",
};

module.exports = {
  MQTT_PORT,
  BROKER_ID,
  URL,
  SERVICE_OPTIONS,
};
